Let me look at the knowledge graph and workflow files to understand the component structure.

**Tool: bash**

```json
{
  "command": "wc -l workflows/*.js",
  "description": "Get line counts for workflow files"
}
```
