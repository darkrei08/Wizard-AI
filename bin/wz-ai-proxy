#!/usr/bin/env bash
# wz-ai-proxy - Manage the Cockpit Tools proxy daemon
# Wraps scripts/wz-ai-proxy.js

if [ -z "$WIZARD_AI_DIR" ]; then
    echo "ERROR: WIZARD_AI_DIR is not set. Please run setup.sh first."
    exit 1
fi

node "$WIZARD_AI_DIR/scripts/wz-ai-proxy.js" "$@"
