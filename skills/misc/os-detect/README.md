# 🔍 The OS Detective (/os)

**What is it?**
MASTER PRE-INSTALL SKILL. Call this BEFORE installing ANY software, package, CLI tool, Python library, Node module, Rust crate, or system dependency. Provides cross-platform OS detection and a unified installation interface (ai-os) that works on Arch, Ubuntu, Fedora, macOS, WSL, Windows and more. This skill is a mandatory gate — no installation happens without it.

**How to remember it:**
Before installing ANYTHING, this detective checks your OS and picks the right package manager. The mandatory gate.

**Slash Command:** `/os`

**Part of:** Tier 1 — Supreme Orchestrator
