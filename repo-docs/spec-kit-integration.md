I'll examine the spec-kit directory structure and the candidate file before writing the page.

**Tool: bash**

```json
{
  "command": "find \"skills/misc/community/spec-kit\" -maxdepth 4 -type f 2>/dev/null | head -100",
  "description": "List spec-kit directory contents"
}
```
