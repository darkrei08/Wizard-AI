# Master Ecosystem Rules
> **MANDATORY PROTOCOL**: You are operating inside a Wizard-AI Enabled Repository.

## Global Rules
1. **GEMINI.md**: You MUST read and strictly obey all protocols defined in the `GEMINI.md` file at the root of the project. It defines the 5-Loop sequential workflow, token compression rules, and the swarm architecture.
2. **AGENTS.md**: You MUST strictly adhere to the formatting and concise guidelines specified in `AGENTS.md` (e.g. caveman style).
3. **Master Skills Integration**: You MUST load and enforce the relevant master skills (`engineering-excellence`, `subagent-driven-development`, `workflow-production-cycle`) whenever they apply to your task.
4. **Token Optimization**: Compress your memory and outputs using `sqz`, `lean-ctx`, or `rtk` to avoid polluting the context window.
